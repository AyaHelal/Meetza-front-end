import React from "react";

/** 
 * Meetings component is now a shell. 
 * The actual UI (MeetingRoom, MeetingChat, etc.) is rendered persistently in AppLayout.js 
 * to ensure audio and socket connections stay alive during navigation.
 */
const Meetings = () => {
  return null;
};

export default Meetings;
