/**
 * Media store – streams, mute state, speaker muted.
 * Currently provided by MediaContext (src/context/MediaContext).
 * Migrate here or wire to Zustand/context for meeting-scoped media state.
 */
export { useMediaContext } from "../../../context/MediaContext";
